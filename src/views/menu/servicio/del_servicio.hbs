{{> navigation }}
{{#each delServicio}}
<div class="container col-md-12 principal d-flex justify-content-center">
    <div class="card bg-light">
        <div class="card-body">
            <h2 class="card-title">¿Desea eliminar este Servicio?</h2>
            <form action="/servicio/del_servicio/{{_id}}" method="post">
                <input type="hidden" name="_id" value="{{_id}}">
                <h3>{{nombre}} </h3>
                <div class="text-right">
                    <button class="btn btn-outline-danger">Eliminar</button>
                </div>
            </form>
            <form action="/info_servivio" method="post" class="d-flex justify-content-center">
                <input type="text" name="id" id="id" value={{id}} hidden required>
                <button type="submit" class="btn btn-outline-danger">Cancelar</button>
            </form>
        </div>
    </div>
</div>

{{/each}}