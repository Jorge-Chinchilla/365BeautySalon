{{> navigation }}
{{#each kai}}
    <div class="container-fluid">
        <form action="/edit_kai" method="post">
            <input type="text" name="id" id="id" value={{_id}} hidden required>
            <button type="submit" class="btn btn-outline-primary">KAI</button>
        </form>
    </div>
{{/each}}

<div class="container-fluid">
    <div class="row query">
        <div class="col-12">
            <div class="">
                <div class="row">
                    <div class="col-lg-6 d-flex justify-content-center">
                        <label class="label">Facturas</label>
                    </div>
                    <div class="col-lg-6 d-flex justify-content-center">
                        <form class="form-inline" action="/filtrarFactura" method="post">
                            <div class="input-group mb-3 ">
                                <input type="text" class="form-control" placeholder="Nombre" name="nombre" id="nombre">
                                <div class="input-group-append">
                                    <button class="btn btn-outline-primary" type="submit">Buscar</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <hr class="regla">
    <div class="row">
        <div class="col-12 product-list">
            <div class="container p-4">
                <div class="row table-responsive-sm">
                    <table class="table table-striped table-hover">
                        <thead>
                        <tr>
                            <th scope="col"></th>
                            <th scope="col">N° Factura</th>
                            <th scope="col">Nombre</th>
                            <th scope="col">Servicio</th>
                            <th scope="col">Total</th>
                            <th scope="col">Fecha</th>
                        </tr>
                        </thead>
                        <tbody>
                        {{#each factura}}
                            <tr>
                                <td>
                                    <form action="/info_factura" method="post">
                                        <input type="text" name="id" id="id" value={{_id}} hidden required>
                                        <input class="btn btn-outline-secondary" type="submit" value="ver">
                                    </form>
                                </td>
                                <td>{{nFactura}}</td>
                                <td>{{nombre}}</td>
                                <td>{{servicio}}</td>
                                <td>{{total}} Lps</td>
                                <td>{{fecha}}</td>
                            </tr>
                        {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>